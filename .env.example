# OpenAdapter Configuration
# Copy this file to .env and configure as needed

# Admin API Key for remote management endpoints (optional)
# If not set, management endpoints are accessible without authentication (local dev only)
# Generate a secure key with: openssl rand -hex 32
ADMIN_API_KEY=your-secret-api-key-here
